<div class="card">
    <div class="card-header">
        <h4 class=" card header">{{title}}</h4>
        <h6 class="card-header"> Created by {{user.username}} on {{format_date created_at}} |
      {{comments.length}} {{format_plural "comment" comments.length}}</h6>
  </div>
  <p class="card-body">{{content}}</p>
</div>